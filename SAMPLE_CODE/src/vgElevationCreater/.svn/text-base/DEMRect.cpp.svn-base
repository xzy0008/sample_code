#include <vgStableHeaders.h>

#include "DEMRect.h"

DEMRect::DEMRect(CPoint point, CSize size, string name)
				:_topLeft(point), _size(size), _name(name)
{

}

DEMRect::DEMRect(int x, int y, int width, int height, string name)
				:_topLeft(x, y), _size(width, height), _name(name)
{

}

DEMRect::~DEMRect(void)
{
}
